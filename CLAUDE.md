# tfl-solver — агент для решения задач по теории формальных языков

Рабочая директория курса ТФЯ (ИУ9, МГТУ им. Баумана). Здесь собраны материалы
преподавателя, учебная литература и 21 репозиторий с решениями старшекурсников.
Цель проекта — агентский цикл (скилл), решающий задачи РК, лаб и экзамена.

## Точки входа

| Документ | О чём |
|---|---|
| [docs/01-KNOWLEDGE-MAP.md](docs/01-KNOWLEDGE-MAP.md) | Что где лежит, чему доверять, как читать PDF |
| [docs/02-TASK-TAXONOMY.md](docs/02-TASK-TAXONOMY.md) | Классы задач: вход → что предъявить → чем проверяется |
| [docs/03-AGENT-PLAN.md](docs/03-AGENT-PLAN.md) | Архитектура агента, цикл S0–S7, критерии L0–L3, план по сессиям |
| [reports/vendor-repos/NOTES.md](reports/vendor-repos/NOTES.md) | Разбор 19 чужих репозиториев: как менялись задания по годам, чужой агент по ТФЯ |
| [docs/OPEN-GAPS.md](docs/OPEN-GAPS.md) | **Недоработки.** Что осталось незакрытым — обновлять при завершении каждой сессии |
| [evals/coverage_report.md](evals/coverage_report.md) | **Счёт покрытия:** куда приходит оракул по каждому классу задач |
| [docs/recipes/](docs/recipes/) | Рецепты по классам задач (`LAB-1`, `LAB-2`, `LAB-3`) |
| [.claude/skills/tfl/SKILL.md](.claude/skills/tfl/SKILL.md) | **Точка входа агента:** приём задачи, цикл S0–S7, правила честности |

## Правила работы

- **Условие — то, что прислал пользователь.** Корпус нужен для другого:
  найти похожую задачу и авторский разбор. Опорный материал берётся только
  из `FormalLanguageTheory/` (репозиторий преподавателя); решения студентов —
  образец оформления, не источник истины: ошибки там встречаются регулярно.
- **Учебный чат — не источник истины.** Выжимка в `corpus/chat/`
  (`FINDINGS.md` — ответы преподавателя и разобранные приёмы,
  `messages.md`, `photos.tsv`). Там вперемешку говорят преподаватель
  и студенты, и по имени аккаунта их не различить.
- **Issues репозитория курса — источник истины.** `corpus/issues/` —
  дословные ответы преподавателя в
  `github.com/TonitaN/FormalLanguageTheory/issues`: #38 (ЛР1, фазз-тест
  и минимальность), #40 (ЛР2, **ПКА через lookahead-регулярки**),
  #35 (ЛР4, семантика ссылок `(?N)` и `\N`).
  Разбалловки: #6 (РК1 2022), #21 (РК1 2023), #25 (**РК2 2023**),
  #9/#27/#34 (**«Фарма»** и банк вопросов). Треды 2022: #6, #7 (Арден с рекурсией
  под звёздочкой, рекурсивный шаг в Н.Ф. Грейбах), #5 (`Annote`
  перед языками состояний), #2 (алгоритм 3 Мартелли–Монтанари).
- **Проверенные работы РК2 с баллами** — `reports/rk2-2026-photos/NOTES.md`:
  89 фотографий, красные пометки преподавателя, разбор того, за что
  снимают баллы и за что ставят 5.
- **Грепаем `corpus/txt/`, а не PDF.** Зеркало строится
  `python tools/extract_corpus.py`; сканы (`kind=SCAN` в `corpus/INDEX.tsv`)
  читаются только рендером: `python tools/extract_corpus.py --render FILE.pdf 1 5`.
- **Каждая гипотеза проверяется исполнением.** Утверждение, не прогнанное через
  оракул, помечается в отчёте `[НЕ ПРОВЕРЕНО]`.
- Отчёты — Markdown с MathJax-формулами (это +1 балл во всех лабах).

## Запуск

```bash
export PYTHONIOENCODING=utf-8    # без этого консоль Windows роняет вывод в CP1251
python -m pytest -q              # pythonpath настроен в pyproject.toml
```

## Состояние

* **S-0 готово** — инвентаризация корпуса, таксономия задач, план.
* **S-1 готово** — ядро оракулов для регулярных языков:
  `tfl/regex.py` (академические регексы, производные Брзозовского),
  `tfl/automata.py` (НКА/ДКА, детерминизация, минимизация, булевы операции,
  эквивалентность со свидетелем), `tfl/myhill.py` (таблица классов,
  обманывающие множества), `tfl/words.py`. 162 теста, все зелёные.
  Сводка по 28 вариантам ЛР2 — `evals/lab2_2025_report.md`.
* **S-2 готово** — позиционный автомат Глушкова и редукция НКА (`tfl/glushkov.py`),
  генератор отчёта `tools/lab2_report.py`, рецепт `docs/recipes/LAB-2.md`,
  образец `reports/lab2-variant-15/`. 254 теста.
* **S-3 готово** — ЛР1 закрыта: ядро SRS (`tfl/srs.py`), извлечение
  всех 28 вариантов (`tools/extract_lab1_variants.py`), генератор отчёта
  (`tools/lab1_report.py`), рецепт `docs/recipes/LAB-1.md`, сводка
  `evals/lab1_2025_report.md`, образец `reports/lab1-variant-20/`.
  304 теста.
* **Семинар 05.09.2026 разобран** — `reports/seminar-2026-09-05/report.md`:
  кодировки и морфизмы (51-Б), системы переписывания образцов (52-Б).
  Оракул нашёл критическую пару `ada`, из-за которой наивная раскодирующая
  система не конфлюэнтна; пополнение двумя правилами её чинит. 598 тестов.
* **S-6 частично** — язык как предикат (`tfl/lang.py`): пересечение
  с регулярным, таблицы различимости Майхилла–Нероуда, подсчёт классов;
  накачка (`tfl/pump.py`): перебор **всех** разбиений, а не удобного.
  Контрольная точка взята из проверенной работы РК2 2025. 577 тестов.
* **S-7 готово** — приём задач: скилл `.claude/skills/tfl/SKILL.md`,
  классификатор и поиск похожих условий (`tfl/intake.py`, `tools/intake.py`),
  размеченный индекс 1531 задачи из корпуса 2021–2025
  (`tools/build_task_index.py` → `evals/tasks/index.jsonl`). 547 тестов.
  **Рецепты написаны для всех 17 классов.**
* **Линейные интерпретации для SRS** — `SRS.find_interpretation`
  и `Interpretation`: каждой букве функция `x ↦ m·x + c`, слово —
  композиция. Берёт удлиняющие правила, против которых армейский порядок
  бессилен в принципе. Встроено в `terminates`. Замер на ЛР1 2025: закрывает
  один вариант из «не выяснено»; перебор матриц 2×2 проверен отдельно
  и не добавил ничего — нужна SMT-постановка.
* **Экзамен и «Аптека» закрыты** — рецепты `docs/recipes/EXAM-1.md`,
  `EXAM-2.md`, `EXAM-3.md`, `PHARMA.md`. Обзор
  `python tools/exam_survey.py` классифицирует 255 вопросов билетов
  **по содержанию**: номер вопроса задаёт слот, а не жанр — самая частая
  тема покрывает меньше 56 %. Новые оракулы:
  `SRS.find_loop` (петля `w →⁺ u·w·v`; цикл — её частный случай, а правило
  с вложенной левой частью циклов не даёт вовсе — закрыло 3 варианта ЛР1
  и сократило свидетелей с пятнадцати слов до двух),
  `tfl.lang.residual_growth` (кривая роста числа классов; каждое число —
  доказанная нижняя оценка, различать надо полку и рост, а не скорость)
  и `tfl/prefix.py` — алфавитные префиксные грамматики по лекции 4 2022:
  коллапсирование букв, перевод в линейную грамматику и сверка
  с прямым переписыванием. Шесть SRS-вопросов билетов из семи закрыты
  механически.
* **Конъюнктивные грамматики закрыты** — `tfl/conj.py` по лекции 11:
  правила `A → Φ₁ & ⋯ & Φₙ`, где конъюнкция это **одно и то же слово,
  разобранное дважды**, а не разные его части. Распознавание — обобщённый
  CYK по неподвижной точке внутри отрезка (нормальная форма не нужна),
  плюс проверка линейности, автомат Треллиса и перевод его в грамматику
  по конструкции лекции. Воспроизведены оба примера лекции — `{(aⁿb)ᵏ}`
  и «язык равенства» `{wcw}`, — и решён билет 23 экзамена 2024
  (строго возрастающие блоки). Стоит 3 балла на РК2 сверх доказательства
  не-КС и три вопроса на экзамене. Рецепт — `docs/recipes/RK2-B.md`, шаг 6.
* **MFA и ref-слова закрыты** — `tfl/mfa.py` по лекции 12: `k`-MFA
  с флагами `o/c/⋄`, симуляция с бюджетом, условие детерминизма,
  ref-слова Шмида. Тонкость, на которой ошибаются: **флаги применяются
  до чтения ленты**, поэтому открытие закрытой ячейки её обнуляет
  (`u' = v`, а не `u·v`). Воспроизведён автомат лекции для `{aⁿ²}`
  вместе с трассой памяти `⟨⟨aᵏ⁻¹,c⟩,⟨aᵏ,o⟩⟩`. Опровержение —
  `tfl.pump.noncsy_by_pumping` (лемма Кампеану–Саломаа–Ю: качается одно
  и то же `y` во **всех** выбранных вхождениях, степень 0 исключена).
  Ещё 3 балла на РК2. Рецепт — `docs/recipes/RK2-B.md`, шаг 7.
* **Real-time свойство** — `PDA.is_real_time` (нет ε-переходов),
  `epsilon_transitions`, `multi_pop_transitions`. Второе не менее важно:
  `Transition` здесь снимает со стека цепочку ради свёрток LR, а курс
  такого не разрешает — «сбросить сразу несколько символов за один шаг
  мы не можем по определению». Приём слияния снимаемых подряд символов
  разобран на `{a²ⁿbⁿ}` тремя автоматами, распознающими один язык:
  снятие двух за шаг, снятие через ε-переход и слияние, дающее оба
  свойства сразу.
* **TRS с переменными** — `tfl/trs.py`: термы, одностороннее сопоставление,
  унификация с проверкой вхождения, переписывание, поиск петли,
  полиномиальные интерпретации. Переменные **объявляются явно**: в корпусе
  две противоположные договорённости (issue #2 — строчные, «Аптека» —
  `variables = [X]`), и угадывать по регистру нельзя. Унификация сверена
  с разобранным примером преподавателя из issue #2. Отдельно —
  `TRS.is_unary` и `as_srs`: система из одноместных символов это
  переписывание строк, и задача F6 «Аптеки» переводится в `tfl/srs.py`
  целиком. Найдено: интерпретация, приведённая в банке вопросов 2024
  для `f(x) → g(x,x)`, правило **не уменьшает**. Унификация сделана
  **алгоритмом 3** (`unify_mm`) через мультиуравнения, общую часть
  и границу, как требует условие ЛР1 2022 («не из википедии!»), и сверена
  с разобранным примером; обычная `unify` считает то же самое другим
  способом, и оба сверены между собой. Есть критические пары,
  `joinable` и локальная конфлюэнтность.
* **Пополнение по Кнуту–Бендиксу для термов** — `TRS.complete` по схеме Юэ:
  очередь уравнений, ориентация рекурсивным путевым порядком (`lpo_greater`,
  `find_precedence`) и **межредукция** после каждого правила; без неё
  система пухнет вчетверо. Аксиомы свободной группы пополняются
  до канонических десяти правил. Старшинство символов — не деталь:
  первое же, ориентирующее аксиомы (`f ≻ e ≻ i`), упирается
  в неориентируемое `i(f(x,y)) = f(i(y),i(x))`, поэтому `complete`
  перебирает кандидатов. Обратный мост `from_srs` читает слово цепочкой
  одноместных термов, и LPO становится доступен строковым системам —
  **замер на ЛР1 2025: ноль вариантов сверх прежних**, над унарной
  сигнатурой порядок вырождается.
* **Проблема соответствия Поста** — `tfl/pcp.py`: поиск по хвосту
  и **метод преподавателя** из ЛР0 2023 — уравнения на числа домино `Mᵢ`,
  решаемые точно (Гаусс плюс Фурье–Моцкин в дробях), и усиление
  уравнениями на пары букв с переменными `Nᵢⱼ`. Воспроизведён разобранный
  в условии пример вместе с его уравнениями `2M₂ = M₃`, `M₁ + 2M₃ = M₂`.
  Тонкость: «в целых **положительных** значениях `Mᵢ`» читать буквально
  нельзя — решение не обязано использовать все домино, и по строгому
  условию можно «опровергнуть» разрешимый экземпляр.
* **µ-выражения Клини** — `tfl/mu.py` по лекции 8: разбор, перевод
  в КС-грамматику, сверка гипотезы предикатом. Тело `µ` тянется вправо
  до упора (`µX.aXb | ε` — это `µX.(aXb | ε)`), а переменная — та, что
  **связана** `µ`, а не та, что заглавная: в лекции они строчные,
  в билете заглавные. Билет 2022 решён и сверен на всех 29 524 словах
  длины ≤ 9.
* **Накачка со степенью от разбиения** — `tfl.pump.factorial_powers`:
  `p!/i + 1`, где `i` — длина накачиваемого куска. Свидетель преподавателя
  с семинара 2024 при фиксированном наборе степеней проваливается,
  с факториальными отбивает все разбиения.
* **Скобочное представление по Хомскому–Шютценберже** — `tfl/schutzenberger.py`
  по лекции 8 (2021): язык как `h(PARENₙ ∩ R)`, построение из грамматики
  в НФ Хомского, проверка `h(L(G′)) = L(G)` и самого равенства. +2 балла
  на задаче 2 РК1. Оракул нашёл в лекции две неточности: «`h([ₙ) = a`,
  для остальных скобок так же» буквально даёт `aaaa` вместо `a`,
  а напечатанное условие `R` равенства `L′ = R ∩ PARENₙ` не обеспечивает —
  контрпример `[₁[₁[₁]₁]₁]₁` на грамматике из одного правила. Чинится
  тремя локальными условиями, `R` остаётся регулярным.
* **Пары зависимостей** — `tfl/deppair.py`: метод Арца и Гизля над
  одноместной сигнатурой. Прямая интерпретация требует, чтобы **каждое**
  правило строго убывало; пары меняют это на «все правила нестрого,
  и строго хотя бы одна пара в каждой циклической компоненте графа
  зависимостей». Диез помечает **корень**, и правила диезов не содержат —
  поэтому шаги цепочки идут строго ниже корня, на этом всё и держится.
  Граф оценивается сверху (правая часть обрезается на первой определённой
  букве после корня), компоненты берутся по отдельности. Редукционная
  пара — аффинные отображения `ℕ^d → ℕ^d`, поиск через Z3,
  **проверка своя** (`DependencyProof.check` пересобирает пары и граф
  заново и считает целыми числами); наружу выдаётся только проверенное.
  Метод **только доказывает**: «не выяснено» не значит «не завершима».
  Замер на ЛР1 2025 (`python tools/lab1_termination.py`): **пять
  вариантов**, из них четыре не брало ничто другое — 4, 8, 12, 19;
  «не выяснено» осталось 11 вместо 15. Вариант 12 показателен: одиннадцать
  букв и 18 пар, но граф распадается на пять маленьких компонент, и только
  поэтому задача решается. Ни одной ошибки в другую сторону: на всех
  11 вариантах с найденной петлёй метод молчит, и это закреплено тестом.
* **Матричные интерпретации** — `tfl/matrix.py`: буква ↦ матрица над ℕ,
  слово ↦ произведение; правило убывает при `M(l) ⩾ M(r)` и строгом
  неравенстве в углу `[0][d−1]`, углы `[0][0]` и `[d−1][d−1]` обязаны быть
  ненулевыми. **Проверка — свой арбитр, от решателя не зависит**; поиск
  идёт через Z3, и он необязателен (`pip install .[smt]`). Замер на ЛР1
  2025: 1 вариант из 16. Куда важнее другое — закрыт вопрос «Аптеки» 2022
  на 5 баллов `fg → gff`, `fh → hg`, не дававшийся ничему.
* **Арктические интерпретации, ограничение совпадениями, удаление
  правил** — `tfl/arctic.py` (max-plus полукольцо: мера слова считает
  самый длинный путь), `tfl/matchbound.py` (метод Гезера–Хофбауэра–Вальдмана:
  вместо меры строится регулярный язык слов **с высотами**, замкнутый
  относительно помеченного переписывания), `tfl/removal.py` (все правила
  роняются нестрого, строго хотя бы одно — оно снимается, и всё повторяется
  на остатке). У всех трёх проверка своя, от решателя не зависит.
  **Замер на ЛР1 2025: ноль** сверх прежних одиннадцати, и это результат,
  а не неудача перебора: и арктика, и ограничение совпадениями дают
  **линейную** границу на длину вывода, а оставшиеся варианты длину
  сохраняют. Контрольная точка в тестах: `ab → ba` сортирует слово
  за `O(n²)` и потому недоступна обоим (решатель отвечает `unsat`),
  хотя обычная матрица размерности 2 её берёт. Удаление правил при этом
  сильнее прямой интерпретации **на той же размерности**: перебором 4808
  случайных систем нашлись две, где прямой интерпретации размерности ⩽ 2
  не существует (доказано решателем), а цепочка из трёх шагов тех же
  размерностей закрывает систему за секунды — `aa → bb`, `ab → a`,
  `aba → aaa`. Граница записана честно: на размерности 3 прямая матрица
  у этого примера всё же находится, за 39 с. На ЛР1 удаление снимает
  часть правил у трёх вариантов (16: 6 из 16, 17: 3 из 5, 21: 1 из 4)
  и застревает на остатке.
* **Полный перебор по длинам** — `SRS.terminates_by_exhaustion`,
  `SRS.cycle_of_length`, `SRS.mirror`. У **не удлиняющей** системы длина
  вдоль вывода не растёт, значит бесконечный вывод обязан застрять
  на одной длине, а там слов конечное число и бесконечность равносильна
  циклу. Поэтому «циклов нет» — **доказательство**, а не «не нашли».
  Варианты 11 и 15 ЛР1 (единственные не удлиняющие среди нерешённых)
  закрыты так на словах длины ⩽ 20 и ⩽ 13.
* **Копредставления полугрупп и групп** — `tfl/presentation.py` по лекции 1
  курса 2026: соотношения ориентируются армейским порядком, система
  пополняется по Кнуту–Бендиксу, проблема равенства решается трёхзначно.
  Тонкость, ради которой всё и разделено: **равенство доказывается без
  конфлюэнтности** — совпадение нормальных форм это уже готовая цепочка
  соотношений, — а неравенство требует полной системы. Классическое
  `⟨a, b | aba⁻¹ = b², bab⁻¹ = a²⟩` признано тривиальным, хотя пополнение
  на нём расходится. Полугруппа Цейтина со слайда 5, записанная
  как SRS, **не завершима** в написанной ориентации: петля `cca → ccae`.
* **Граф Кэли и действие группы** — `tfl/cayley.py` по лекции 1 курса 2026
  (пункты 2 и 4 списка к лекции 12 сентября). Граф Кэли строится обходом
  нормальных форм и **является детерминированным автоматом**: вершины —
  элементы, рёбра — умножение справа, финальная единица даёт распознаватель
  проблемы равенства. Отсюда посчитана лёгкая половина теоремы Анисимова:
  вычет по слову `u` это множество слов, равных `u⁻¹`, поэтому число классов
  Майхилла–Нероуда **равно порядку группы** (у `S₃` 6 и 6). Конечность
  моноида у пополненной системы **разрешима**: элементы — это неприводимые
  слова, они образуют регулярный язык, и конечность проверяется отсутствием
  цикла в автомате. Поэтому «группа бесконечна» здесь доказательство,
  а не «обход не закрылся». Действие задаётся перестановками образующих
  и проверяется по соотношениям; взамен оно умеет то, чего не умеет
  переписывание, — доказывать **неравенство** слов без полной системы.
* **Переписывания по Конвею–Кробу** — `tfl/conway.py`: классические
  тождества регулярной алгебры плюс групповые для циклических групп,
  обход в обе стороны, каждый шаг сверяется с оракулом эквивалентности.
  Термина в лекциях нет, он только в разбалловке РК1 2023. Групповые
  тождества видно на примере: `(aa)*|a(aa)*` одними классическими
  не сводится к `a*` даже при обходе всех выражений размера до 22,
  а с тождеством для группы порядка 2 — сразу.
* **Jumping Lemma для DMFL** — `tfl.mfa.nondmfl_by_jumping` по лекции 12.
  Оракул нашёл, что **напечатанная квантификация не работает**: при чтении
  «`∃pₙ, vₙ`» условию удовлетворяет `{aⁿbⁿ}` (свидетель `p = a²ᵐbᵐ`,
  `v = bᵐ`) — тот самый язык, который лекция разбирает как не-DMFL.
  Разбор сходится при чтении «`∀pₙ, vₙ`». Реализованы оба.
* **`NL*` закрыт** — `tfl/nlstar.py` по слайду 18 ЛР3 2023: активное
  обучение **НКА**. Условия на таблицу — про включение строк, а не
  про равенство: состояние нужно, только если вычет не собирается
  объединением меньших. Эталон строится независимо от обучения, прямо
  по определению вычетов (`canonical_rfsa`), и на 200 случайных ДКА
  расхождений ноль. Замеры против ожиданий: выигрыш по состояниям
  экспоненциален (ДКА `2ⁿ` против RFSA `n+1`), но **запросов `NL*`
  тратит больше**, чем `L*` — 75 случаев из 200 против 20; на планарных
  лабиринтах ЛР2 выигрыша нет вовсе. Стратегия префиксов для `NL*`
  не сходится, и это следствие прочтения слайда, а не дефект: нулевая
  строка не базисна, начальных состояний не остаётся. Оба прочтения
  реализованы, застой распознаётся и называется.
* **ЛР5 закрыта** — `tfl/glr.py`: Generic-разбор гиперстеком по
  произвольной КС-грамматике. Конфликт таблицы — не отказ, а ветвление.
  Все варианты работы: SLR(1), LR(0), нисходящий LL(1), плюс соло-версия
  с древовидным стеком. Упакованный лес (бонус +6) строится для
  восходящего разбора, и число разборов берётся **из него**: на
  `S → SS | a` выходят числа Каталана `1, 1, 2, 5, 14, 42`, сверенные
  с независимым перебором деревьев. Замер, ради которого работа
  и делается: разборов экспоненциально много, а вершин графовидного
  стека линейно (`+3` на букву), древовидного — вдвое на каждую букву.
  Ловушка нисходящего разбора: вершину нельзя метить символом и позицией
  — один нетерминал на разной глубине склеивается, и на `S → aSb | ab`
  со словом `abb` вырастает петля, принимающая слово не из языка.
  Вершина метится точкой возврата. **Оба бонуса взяты.** Лес для
  нисходящего разбора бинаризован по Скотту–Джонстону: узел заводится
  на пункт `A → α • β` при `|α| ⩾ 2`, на один символ не заводится
  вовсе, пустая правая часть даёт лист `(ε, i, i)`; в дерево узлы
  бинаризации не текут. Конъюнктивные грамматики (**+4**) разбираются
  тем же гиперстеком: таблица берётся у дизъюнктивного послабления
  (`&` → `|`), а **свёртка конъюнкта вершину не кладёт** — кладёт её
  правило целиком, когда все конъюнкты собраны над одной и той же
  вершиной; сходятся они к одной вершине потому, что пункты `A → •Φⱼ`
  лежат в одном состоянии LR, и потому же бонус даётся только
  за графовидный стек. Сверки: лес GLL против перебора деревьев
  на 2033 словах, конъюнктивный разбор против CYK на 511 словах.
  Попутно найден дефект в уже написанном коде — потомки в лесу лежали
  справа налево; счёт разборов этого не видит, поймал `yield_of`.
  Рецепт — `docs/recipes/LAB-5.md`.
* **База структурных образцов** — `tfl/hints.py`: 23 записи «язык такого
  вида → вероятный вердикт → чем доказывать». Идея из чужого агента,
  содержание своё, и главное — **разметка по надёжности**: две записи
  чужой базы неверны как записаны и хранятся вместе с исполняемым
  контрпримером («реверс DCFL — не DCFL» — реверс регулярного языка
  регулярен; «дополнение известного языка — DCFL» с примером
  `¬{aⁿbⁿcⁿ}` — DCFL замкнут относительно дополнения, значит и сам
  язык был бы детерминирован). Встроена в `intake`. В скилл добавлена
  инверсия гипотезы.
* **РК2 вариант 28 задача 1 решена** — `tools/rk2_variant_28.py`.
  Слово лежит в языке тогда и только тогда, когда букв `a` хотя бы одна
  и найдётся `n`, `⌈k/2⌉ ⩽ n ⩽ |w|_b`, при котором не менее `n` букв `b`
  стоят левее последней `a` и не правее `n`-й. Прежним трём инвариантам
  не хватало пятого: **последний слот не пополняется** правилами
  `ba² → ba` и `ab → ba`, а в базисе он пуст. Достаточность предъявлена
  выводом из базиса, сверенным пошагово с самой системой на всех 5593
  словах длины ⩽ 12. Язык не регулярен и не КС; накачка на `aᵖbᵖaᵖ`
  разбирается символьно, то есть при любом `p`.
* **Расширенные регулярки ЛР2 закрыты** — `tfl/lookaround.py`: разбор
  синтаксиса условия (`.`, `τ⁺`, `τ?`, классы и дополнения, обязательные
  `^` и `$`, все четыре проверки), распознаватель (**+1 балл**) и перевод
  в ПКА (**+1–2 балла**). Семантика взята **позиционной**: печатные
  равенства заданы для верхнего уровня, а в собственном примере
  преподавателя проверка стоит под звёздочкой и смотрит на остаток
  **всего** слова. Совпадение с равенствами на верхнем уровне
  не предполагается, а считается операциями над автоматами и сверяется.
  Приёмочная точка — её же разбор: «количество букв `a` между каждыми
  двумя `b` чётно, причём перед последней их ненулевое количество»
  сходится с нашим распознавателем на всех 1023 словах длины ⩽ 9.
  Найдено: **`$` внутри проверки не украшение** — `(?=τ$)` требует лечь
  на весь остаток, и без этого язык раздувается; и **ретроспектива
  в условии расходится с общепринятой** — печатное `(τ₀ ∩ (τ₁.*))τ₂`
  требует, чтобы префикс с `τ₁` **начинался**, а PCRE — чтобы кончался.
  Свидетель `^ab(?<=a).*$` на слове `ab`. Реализованы оба прочтения.
* **Нетривиальные инварианты ЛР1** — `SRS.monoid_invariants`
  и `SRS.matrix_invariants`: гомоморфизмы в моноид преобразований `k` точек
  и в моноид матриц над `Z_m`. Счётный инвариант видит только количество
  букв, гомоморфизм — **порядок**, и за это условие платит до +2 баллов.
  Найденное **доказано** для всех слов сразу: `φ(lhs) = φ(rhs)` влечёт
  сохранение при переписывании в одну строку. Нетривиальность проверяется
  механически: инвариант не сводится к счёту букв ровно тогда, когда
  различает два слова с одинаковым набором букв. Показательная пара —
  `aab → b`: счётные инварианты есть, но `ab` и `ba` они не различают,
  а `[a] = 10`, `[b] = 00` различает (перестановка и константа
  не коммутируют). Обратное так же полезно: у `ab → ba` таких инвариантов
  нет и быть не может — правило разрешает любую перестановку, значит
  образы обязаны коммутировать. **Замер: 17 вариантов из 28**
  (`python tools/lab1_invariants.py`). Граница: вариант 20 не берётся
  ничем из малого — ни счётными по модулю 2, 3, 5, 7, ни `T₂`, `T₃`,
  ни `M₂(Z₂..Z₅)`, ни `M₃(Z₂)`; у него 19 правил, и они связывают образы
  слишком туго.
* **Курс 2026 стартовал** — `reports/course-2026/NOTES.md`. Репозиторий
  преподавателя подтянут до `681996b`, материалы прошлых лет убраны
  в `2025/` (зеркало корпуса переименовано соответственно), появился
  конспект лекции 1 от 05.09.26. Преподаватель прямо пишет, что версия
  2026 года отличается от всех предыдущих по набору и структуре
  материала. **Условий ЛР и РК 2026 нет.** Лекция 1 на две трети —
  то, что в проекте уже сделано; новое это алгебраический фронт:
  копредставления, полугруппа Цейтина, лемма о пинг-понге, граф Кэли,
  действие группы. Прямое задание к лекции 12 сентября — «Пример 1.78
  из лекций ГН», показать, что из соотношений следует коммутативность;
  **условия в корпусе нет**, оракул готов, соотношения надо спросить.
* **S-8 готово — набор для оценки.** `evals/suite.py`: 60 задач корпуса,
  у каждой исполняемая проверка и записанный исход. Прогон —
  `python tools/eval_suite.py`, отчёт — `evals/coverage_report.md`.
  Счёт на 2026-09-09: **42 решено, 13 частично, 5 вручную**, 16 классов.
  Граница
  между «решено» и «частично» проведена по **вопросу условия**, а не
  по объёму отработавшего кода: проверка предъявленной интерпретации —
  это «частично». Все четыре «вручную» — границы разрешимости
  (детерминированность языка, замкнутость классов), а не дыры в коде.
* **Активное обучение закрыто** — `tfl/lstar.py` по ЛР2 2024: таблица
  наблюдений с условиями замкнутости и непротиворечивости дословно
  по слайду 5, построение ДКА по таблице, формат запроса об эквивалентности
  со слайда 7 (с **расширенной частью** — прямое требование issue #31).
  Обе стратегии обработки контрпримера и оба характера МАТа из issue #31.
  `tfl/maze.py` — планарный лабиринт как цель обучения. Новый класс `MAT`
  в таксономии, рецепт `docs/recipes/MAT.md`.
  Замеры, а не предположения: ни одна стратегия не выигрывает всегда
  (суффиксы берут 10 лабиринтов из 12, префиксы — регулярку `(a|b)*abb`);
  «издевательский» МАТ дороже втрое по запросам, но на `(aaa)*b`
  укладывается в два раунда вместо трёх. **Ортогональный лабиринт
  не реализован**: при буквальном чтении условия язык не регулярен,
  и это вопрос преподавателю. **BF-Рефал и BF-Лисп закрыты** —
  `tfl/bflex.py`: генератор автоматов лексем, проверка ограничений ТЗ
  **операциями над языками** (пересечения, склейки, конечность,
  крайние буквы), сборка автомата лексера. Цель регулярна только потому,
  что вложенность скобок ограничена, и у Рефала счётчик у каждого
  из трёх типов свой. Сверка двусторонняя: автомат собирается операциями
  над автоматами, а программы порождаются **выводом по грамматике**.
  Найдено: ТЗ можно выполнить целиком и получить **вырожденную** цель —
  у Лиспа при `[atom]` = «длиннее трёх, начинается на 1, кончается на 2»
  и скобках `{1}`, `{2}` конструкция `[lbr][atom][dot][atom][rbr]` сама
  является атомом, и скобочное правило не добавляет ничего. Лечится
  разведением ролей букв, и оно же решает вопрос скорости: при общем
  подалфавите детерминизация на вложенности 2 стоит 8.2 с и 709
  состояний против 0.2 с и 375.
* **Кодировки и морфизмы закрыты** — класс `CODE`, рецепт
  `docs/recipes/CODE.md`. `tfl/code.py`: **алгоритм Сардинаса–Паттерсона**
  решает однозначность декодирования **точно**, а не перебором — морфизм
  `{0, 01, 11}` из задачи 4 семинара теперь доказанно инъективен, а не
  «проверен до девятой длины»; при отрицательном ответе предъявляется слово
  с двумя разборами. Плюс задержка раскодирования кривой роста (полка
  против роста, как в `tfl/lang.py`). `tfl/pattern.py`: переписывание
  **образцов** `aXb → bXa` с явно объявленными переменными, а вместо
  неразрешимых завершимости и конфлюэнтности — арбитры `check_measure`
  и `check_invariant` для предложенных человеком меры и инварианта.
  Моделирование образца маркером-долгом сверено в обе стороны; две ловушки
  закреплены тестами: в сравнение идут только строки без маркеров,
  и потолок длины у обеих сторон обязан быть одинаковым.
  **Критические пары для образцов** — `overlaps`, `locally_confluent`,
  `joinable`. У образцов иначе, чем у строк, три вещи: наложений
  бесконечно много (переменная берёт кусок любой длины, и вопрос сводится
  к словесным уравнениям), **один редекс разбирается двумя подстановками**
  (`aXb` на `abb` даёт и `bab`, и `bba` — у строк такого не бывает),
  а наложение **внутри переменной** критическим не является, кроме случая
  нелинейной левой части: у `aXbX → c` копии обязаны совпадать, и разница
  измерена — 2 наложения против 17. Положительный ответ с переменными
  остаётся «не выяснено», отрицательный доказателен; без переменных
  перебор полон, и ответ сверен с `tfl/srs.py` на семи системах.
* **ЛР4 закрыта** — `tfl/extre.py`: разбор расширенных регулярок,
  нумерация групп по открывающей скобке, все три ограничения условия
  и анализ инициализированности ссылок. Воспроизводятся **все двенадцать**
  вердиктов преподавателя из issue #35. Каркас (`skeleton_cfg`) точен без
  ссылок на строку и даёт язык сверху с ними. Рецепт `docs/recipes/LAB-4.md`.
  Попутно проверена опечатка в условии: `(a(?1)b|c)` задаёт `{aⁿcbⁿ}`,
  а не `{aⁿcⁿbⁿ}` — последний вообще не КС. 741 тест.
* **РК1 закрыт** — рецепты `docs/recipes/RK1-A.md`, `RK1-B.md`, `RK1-C.md`
  по авторскому разбору пробной работы и условиям 2025. Новые оракулы:
  `tfl.lang.counter_dfa` (условие на счётчики подслов: обход состояний
  «последние k−1 букв + значение» конечен ровно тогда, когда разность
  ограничена, поэтому завершившийся обход **доказывает** регулярность
  и выдаёт ДКА) и `tfl/tree.py` — древесные языки: термы, контексты,
  детерминированный восходящий автомат, оценка снизу на число классов
  различающими контекстами. Древесные языки стоят минимум в шести
  вариантах РК1 2025 из четырнадцати. 713 тестов.
* **1-однозначность** — `tfl.glushkov.is_one_unambiguous` и
  `nondeterminism`: детерминирован ли автомат Глушкова и в каких именно
  позициях разбор недетерминирован. 2 балла на РК2 2023, часть задачи 4
  РК1 2023, вопрос «Фармы». Вывод односторонний: детерминированность
  доказывает 1-однозначность языка, обратное — нет.
* **Синтаксический моноид и лемма Огдена** — `tfl/monoid.py`
  (трансформационный моноид ДКА, синтаксический моноид языка, SRS-представление,
  апериодичность по Шютценберже) и `tfl.pump.noncf_by_ogden` в формулировке
  курса (лекция 6): отмеченные буквы **во всех трёх** частях одной из двух
  троек. Оба приёма приносят баллы: моноид — +1 на задаче 2 РК1 сверх НКА,
  Огден — вопрос РК1 2021. Источники — `reports/vendor-repos/NOTES.md`.
  691 тест.
* **RK2-A и RK2-B закрыты** — рецепты `docs/recipes/RK2-A.md`
  и `RK2-B.md`. Новые оракулы: `CFG.residues` / `CFG.counting_condition`
  (числовой инвариант грамматики против числового условия — доказывает
  пустоту языка неподвижной точкой, а не перебором), `SRS.closure`
  (замыкание базиса), лемма Ю в `tfl/pump.py` (`defeats_dcfl`,
  `nondcfl_by_pumping`) и `tfl.lang.one_letter_noncf` — следствие
  теоремы Париха «над однобуквенным алфавитом регулярные и КС-языки
  совпадают». 663 теста.
* **ПКА закрыт** — `tfl/afa.py`: альтернирующие автоматы по лекции 6,
  обе конструкции из issue #40 (`conjunction` — И-ветвление в стартовой
  вершине, `with_lookahead` — рекурсивный инвариант), дополнение по
  теореме 5, перевод в НКА по теореме 6, дерево прогона. Оба примера
  преподавателя сверены с самими lookahead-регулярками. Рецепт —
  `docs/recipes/LAB-2.md`, §4. 647 тестов.
* **S-5 готово для РК2** — атрибутные грамматики (`tfl/attr.py`): нотация
  курса (`S0`/`S1`, `:=` и условия), синтезируемые и наследуемые атрибуты,
  исчерпывающий перебор деревьев вывода, счёт по неподвижной точке для
  грамматик с ε-циклами, проверка гипотез об атрибутах
  (`check_attribute`). Рецепт `docs/recipes/RK2-C.md`. 624 теста.
* **S-4 готово** — ЛР3 закрыта: КС-грамматики (`tfl/cfg.py`): чистка,
  First_k/Follow_k, LL(1), LR(0)/SLR(1), НФ Хомского; разбор
  (`tfl/parse.py`): Эрли, CYK, перечисление языка, беспрефиксность;
  магазинные автоматы (`tfl/pda.py`): симуляция с бюджетом, детерминизм,
  CFG↔PDA; регулярные аппроксимации сверху и пересечение по Бар-Хиллелу
  (`tfl/approx.py`). Рецепт `docs/recipes/LAB-3.md`, генератор
  `tools/lab3_report.py`, образец `reports/lab3-variant-01/`. 523 теста.
  См. таблицу сессий в `docs/03-AGENT-PLAN.md`, §7.

Тонкости, уже стоившие ошибки и закреплённые тестами:

1. `len(dfa)` — состояния без ловушки, `dfa.class_count()` — классы
   Майхилла–Нероуда вместе с ловушкой. Путаница даёт расхождение на единицу.
2. В `tfl/srs.py` почти всё неразрешимо, поэтому проверки возвращают не `bool`,
   а `Verdict` с тремя исходами. Пустое пересечение достижимых множеств
   доказывает несходимость **только** при `Search.exact`; без этого флага
   вывод неправомерен. `bool(Verdict)` намеренно бросает исключение.
3. Для оценки размера НКА нужна **треугольная** версия теоремы
   Глайстера–Шаллита (`extended_fooling_set`); симметричная систематически
   занижает оценку.
4. **Пересечение с регулярным работает в одну сторону.** Из «`L ∩ R`
   не лежит в классе» следует «`L` не лежит в классе»; обратный вывод
   неправомерен. Прямая пометка преподавателя на проверенной работе:
   «оно может привести к переходу NCFL → DCFL в общем случае».
5. Свойство грамматики — не свойство языка. `is_ll1()`, `is_deterministic()`
   и подобные говорят только про предъявленный объект; функций
   `language_is_ll1()` и «детерминирован ли язык» нет намеренно, эти
   вопросы неразрешимы.
6. **Цикл — частный случай петли, и его одного мало.** Правило с левой
   частью, вложенной в правую (`abb → abbb`), не возвращает ни одно слово
   к себе: слово растёт, не повторяясь. Ищется петля `w →⁺ u·w·v`.
7. **В кривой роста числа классов различать надо полку и рост,
   а не скорость роста.** У `|w|_a = |w|_b` классов ровно `ℓ+1` —
   рост линейный, а язык нерегулярен. Проверка «удваивается ли»
   такие языки объявляла регулярными.

## Как приходит задача

Пользователь присылает **условие целиком** — текстом или текстом с фотографией.
Номер варианта не нужен и спрашивать его не надо. Форму контроля
(`РК1`, `РК2`, `ЛР1`…`ЛР4`, `экзамен`, `Аптека`) пользователь может пометить,
но не обязан.

```bash
python tools/intake.py --file task.txt --hint РК2
```

Точка входа для агента — [.claude/skills/tfl/SKILL.md](.claude/skills/tfl/SKILL.md).
Если решения получить не удалось, выдаются **наработки**: что установлено,
что опровергнуто со свидетелями, что осталось проверить.
