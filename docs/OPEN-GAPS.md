# Незакрытые задачи tfl-solver

Здесь перечисляется только реально незакрытое. Текущие числа тестов и eval
находятся в `PROJECT-STATE.md`; история изменений остаётся в Git, но не
включается в переносимую предметную память.

Версия 1.0 завершена и заморожена в режиме сопровождения. Пункты ниже честно
фиксируют границы знания и отложенную проверку, но не образуют активный план
расширения архитектуры.

## P0 — события курса, на которые нужно реагировать

### Актуальный курс 2026

Формат контроля записан в `docs/COURSE-2026.md`, runtime умеет отдельно
маршрутизировать РК, лабораторные по содержанию и аудит ИИ-решения. Для
дальнейшей работы нужны внешние материалы, которых в корпусе пока нет:

- условия всех четырёх лабораторных и точные критерии по 7 баллов;
- номер лабораторной с защитой, цена ЛР4 и точное правило замены после
  неудачной защиты;
- полная заранее известная база карточек РК и правила таймера;
- примеры части 1 экзамена и «нейрослопа» части 2 с критериями разбора;
- регрессионный eval помощника: задержка и точность короткого объяснения,
  полнота автономного проекта ЛР и качество самостоятельной устной защиты.

Big Pharma исключена из актуального плана. Код, рецепт и старые задачи
`PHARMA` остаются историческими регрессиями и не должны влиять на стратегию
набора баллов 2026.

При появлении полного условия любой ЛР-2026 нужно **сразу решать ЛР и готовить
автономный сдаваемый проект**, а не продолжать общую разработку агента.

Агент не будет использоваться прямо на РК или экзамене. Таймер нужен только
для тренировочной имитации; инженерную скорость ответа оптимизируем ради
удобства подготовки, а не ради скрытого live-подсказчика.

### Автономные проекты лабораторных после публикации

`docs/LAB-PROJECT-STANDARD.md` и команды `tfl lab init/check` задают
структуру без внешних зависимостей и проверяют импорты, декларации
зависимостей, заглушки и stdlib-тесты. Для каждой опубликованной работы нужно:

- применить цикл к каждой из четырёх опубликованных ЛР и сохранить
  task-specific регрессии, а не только общий scaffold;
- проверить каждый готовый проект на чистой машине одним системным Python;
- сопоставить README, формат ввода/вывода и отчёт с фактической рубрикой;
- проверить, что пользователь может объяснить код, инвариант и свидетель без
  доступа к агенту.

### Необязательная дополнительная валидация 1.0

Holdout v1 из семи задач 2021–2025, JSON Schema, runners Codex/Claude и scorer уже
лежат в `evals/agent_holdout/` и `tfl/agent_eval.py`. Осталось:

- снять оставшиеся пять случаев полного baseline v1 и разобрать провалы;
- проверить эффективность лимита на полном baseline: первый smoke до него
  потребовал 15 команд/197,57 с/415365 входных токенов, второй с лимитом —
  9 команд/227,53 с/294894 токена; одной разнотипной пары недостаточно для
  вывода о wall-clock и стоимости;
- повторить Claude smoke после появления API-ключа или разрешения Claude Code
  в организации: adapter и event-stream parser готовы, но текущий live-запуск
  останавливается с 403 `oauth_org_not_allowed` до первого tool call;
- провести ручную предметную проверку финальных доказательств: текущий scorer
  надёжно ловит маршрут, пустые свидетельства и завышенный статус конечного
  перебора, но смысловые якоря не заменяют математического рецензента;
- после публикации создать v2 со слепыми задачами 2026 года.

## P1 — сопровождение новых и необработанных источников

Новые задачи семинаров и новые лекции добавляются периодически: сохранить
оригинал под `references/`, обновить безопасное зеркало/индекс, сверить
маршрутизацию и добавить минимальную регрессию. Полный пересмотр архитектуры
нужен только если новый материал действительно не покрывается существующими
рецептами и оракулами.

### Вторичные материалы

Вся полученная подборка разобрана: предметные уточнения, методы и ошибки
перенесены в `corpus/knowledge/`, дубли и непредметные данные отброшены.
Сырые журналы, изображения людей, списки, оценки и персональная атрибуция не
входят в проект. Новые вторичные материалы обрабатываются по
`corpus/knowledge/PRIVACY.md`.

### Учебные сканы

- Детально разобрать 49 страниц `tfl_lec_sem.pdf`.
- Просмотреть четыре SCAN-записи из `corpus/INDEX.tsv` и три PDF с ненадёжным
  OCR (`tfl_rk1_var_2_new`, `tfl_rk2_var_8`, конспект лекций).
- Согласовать старое упоминание восьми сканов с дедуплицированным индексом:
  сейчас в нём 117 PDF, из них 4 SCAN.

## P1 — предметные пробелы

### ЛР1: завершимость SRS

Не выяснены варианты 10, 16, 17, 18, 21, 22, 24, 26, 27. Для вариантов 11 и 15
есть только полные проверки на фиксированных длинах. Следующие направления:

- арктические интерпретации с отрицательными элементами и парами зависимостей;
- RFC match bounds и quasi-deterministic completion;
- SMT-поиск на больших доменах;
- явный анализ квадратичных length-preserving выводов.

### Минимальная эквивалентная SRS

Нет полного метода для конечного множества нормальных форм. Кнут–Бендикс
расходится на варианте 20, а компактный вторичный кандидат пока
не воспроизведён независимо. Текущая стабилизация числа классов остаётся
эвристикой.

### ЛР4

- Реализовать точную принадлежность для строковых backreference; текущий
  скелет расширенной регулярки теряет часть lookaround-семантики.
- Добавить полноценный сценарий нечётных вариантов ЛР4 с атрибутными
  грамматиками поверх существующего `tfl/attr.py`.

### Шаблонные системы переписывания

Нужны символьная унификация и решение простых словных уравнений. Без них
положительное доказательство локальной конфлюэнтности правил с переменными
остаётся ручным.

### Теория 2026

- Механизировать применение ping-pong lemma.
- Восстановить точное условие примера 1.78; в доступных материалах есть только
  ссылка на номер.
- После каждой лекции сверять новые определения с прежней нумерацией курса.

## P2 — улучшения покрытия

- Улучшить поиск нижних границ минимальности NFA, когда fooling set не совпал
  с построенным автоматом.
- Сопоставить схемы ЛР5 через неподвижные точки с RNGLR.
- Провести обещанную независимую сверку десяти грамматик в Grammophone.
- Улучшить положительные стратегии для Conway–Krob и неограниченного delay в
  задачах кодирования.
- Архивная `PHARMA` F8 остаётся ручной; для курса 2026 это не приоритет.
- Многосимвольные терминалы, LR(1)/LALR(1), TAG и VPL добавлять только при
  появлении реального задания, а не ради формального охвата лекций.

## Вопросы, требующие официального уточнения

1. Семантика lookbehind в курсе относительно стандартной семантики.
2. Квантификация в напечатанной Jumping Lemma.
3. Нулевая строка и стратегия префиксов в NL*.
4. Буквальная семантика ортогонального лабиринта.
5. Предполагаемый термин в вопросе Conway–Krob.
6. Точная конструкция LL(1)-аппроксимации.
7. Исправления к формулировке представления Шютценберже.
8. Недостающее продолжение материала про алгоритм Блюма–Коха.

## Намеренные границы

Следующие ручные исходы сами по себе не являются дефектом агента:

- доказательство детерминированности языка или невозможности real-time PDA в
  общем случае;
- изобретение контрпримера к замыканию без ограниченного семейства кандидатов;
- общий тест семилинейности образа Париха по произвольному предикату;
- вывод общего утверждения из конечного перебора.

Для таких задач корректный результат — содержательный ручной план,
проверенные частные случаи и честный статус.

## Порядок следующей работы

1. Ждать публикации первого полного условия ЛР-2026; после получения делать
   саму ЛР, её тесты, описание, раскрытие ИИ и подготовку к защите.
2. При появлении новых семинарских задач или лекций точечно обновлять корпус,
   индекс, документацию и регрессии.
3. Базу карточек РК и экзаменационные примеры добавлять по мере публикации для
   подготовки, а не для использования во время контроля.
4. Остальные исследования и полный holdout продолжать только по явному запросу,
   при обнаружении дефекта либо когда они нужны конкретной новой задаче.
