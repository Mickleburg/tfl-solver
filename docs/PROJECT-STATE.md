# Актуальное состояние tfl-solver

Дата среза: **2026-09-13**. Это единственный документ с текущими числовыми
показателями. История разработки доступна через Git; отдельный архив сессионных
журналов намеренно не публикуется.

Статус: **версия 1.0, базовая разработка завершена, режим сопровождения**.
Новая архитектурная работа не планируется без обнаруженного дефекта или явного
запроса. При публикации условий ЛР-2026 приоритет немедленно переходит от
разработки агента к выполнению соответствующей лабораторной.

## Цель

Переносимый учебный помощник быстро объясняет любую теорию ТФЯ, принимает
полное условие задачи текстом или фотографией и выполняет цикл:

```text
условие → классификация → рецепт → формализация → гипотеза ↔ Python-оракул
        → доказательство → отчёт с явной границей проверенного
```

Результатом служит либо оформленное и проверенное решение, либо содержательный
частичный результат: доказанные факты, опровергнутые гипотезы и оставшиеся
обязательства.

Для лабораторной результатом становится отдельный автономный проект: только
Python 3.11+ и стандартная библиотека, без импорта `tfl`, сети и внешних
пакетов. В него входят полное условие, реализация, `unittest`, README, описание
корректности и фактическое раскрытие использования ИИ. На РК и экзамене агент
не используется; он заранее готовит к самостоятельному решению и объяснению.

Форма контроля 2026 зафиксирована в `docs/COURSE-2026.md`: четыре
лабораторные с раскрытием использования ИИ, РК по известной базе под таймером,
двухчастный экзамен с аудитом ИИ-решения и собеседование для оценки 5. Big
Pharma в текущем курсе отсутствует.

## Готово

- Единый канонический skill для Codex и Claude Code: `skills/tfl/SKILL.md`.
- Репозиторий оформлен как устанавливаемый плагин обеих платформ: переносимый
  `plugin.json`, совместимые манифесты `.codex-plugin/` и `.claude-plugin/`,
  а также marketplace-каталоги для установки напрямую из GitHub.
- Старые `.agents/skills/tfl-solver/` и `.claude/skills/tfl/` оставлены как
  тонкие адаптеры, поэтому существующая ручная установка продолжает работать.
- Устанавливаемый из GitHub wheel 1.0 включает skill, plugin-манифесты,
  лицензию Apache-2.0, рецепты, документацию, корпус, eval и Python-оракулы;
  чистая пакетная установка проверена отдельно от исходного дерева.
- Переносимая CLI: `tfl` / `py -3 -m tfl` / `tfl-agent` с командами `setup`,
  `root`, `doctor`, `intake`, `lab`, `srs`, `eval`, `holdout`.
- Стабильная команда `tfl srs critical-pairs` принимает повторяемые `--rule`
  или UTF-8-файл и выдаёт машинно-разбираемый ASCII либо JSON без зависимости
  от кодовой страницы Windows. Точный пример с `aab -> ba`, `aaa -> ab`
  закреплён unit-, CLI- и plugin-регрессиями.
- `tfl setup` безопасно ставит пользовательские точки входа: Claude Code
  `/tfl`, Codex `$tfl-solver` и `/prompts:tfl`; чужие файлы без `--force` не
  перезаписываются.
- Стандарт автономной лабораторной и `lab init/check`: безопасная заготовка,
  аудит структуры, заглушек, импортов/деклараций зависимостей и запуск тестов
  только стандартной библиотекой.
- Быстрый режим для однозначных механических задач: один стабильный оракул и
  ручная проверка полноты без обязательного запуска диагностики, intake,
  поиска по корпусу или загрузки длинного рецепта. Сырой stdout цитируется
  буквально; любая таблица, сортировка или перевод явно помечается как
  переформатирование.
- 18 рецептов: LAB-1…5, RK1-A…C, RK2-A…C, EXAM-1…3, EXAM-ERROR, MAT, CODE,
  PHARMA. Последние исторические `LAB-5`/`PHARMA` не считаются формой 2026.
- Python-оракулы для регулярных языков и автоматов, SRS/TRS, CFG/PDA,
  атрибутных грамматик, AFA и расширенных регулярок, GLR/GLL, L*/NL*,
  моноидов, PCP, копредставлений, автоматов Мили/Мура и сетей
  Мак-Каллока–Питтса.
- Входной индекс задач 2021–2025 и поиск похожих условий.
- Все локальные первоисточники собраны под `references/` по уровню доверия.
- Визуально проверены все 49 страниц сводного конспекта и 33 страницы
  SCAN/OCR-наборов; индекс различает `TEXT`, `OCR` и `SCAN`, а безличная
  предметная выжимка сохранена в `corpus/knowledge/SCAN-REVIEW.md`.
- Вторичная предметная память обезличена: полезные уточнения и методы находятся
  в `corpus/knowledge/`, а журналы общения и персональная атрибуция удалены из
  текущего дерева и переносимой сборки.
- Сценарий editable-установки, `doctor` и `intake` проверен в чистом клоне без
  локальных `references/` и при запуске из другой рабочей директории.
- Заморожен holdout v1 из 7 задач 2021–2025; CLI умеет запускать Codex в
  read-only режиме или Claude Code с узким allowlist инструментов и оценивать
  маршрут, формализацию, реальные команды оракулов, статусы и итоговый ответ.
- `tfl.extre.exact_matches` точно проверяет строки для capture/backreference,
  recursive expression reference и lookahead; режим `stateful=True`
  поддерживает многопроходную семантику архивных вариантов 2025.

## Проверенный срез

| Проверка | Результат |
|---|---|
| Unit/integration tests | 1507 passed, 1 skipped |
| Codex plugin | manifest valid, local marketplace parsed |
| Claude Code plugin | plugin + marketplace strict validation passed |
| Wheel 1.0 | clean venv install and `doctor` passed |
| Исполняемый eval | 63 задачи |
| Eval: решено | 46 |
| Eval: частично | 13 |
| Eval: вручную | 4 |
| Eval: ошибки | 0 |
| Явно представленные классы eval | 18 из 18 |
| Codex holdout smoke | 2/2 PASS, 10/10 |
| Claude holdout smoke | BLOCKED: `oauth_org_not_allowed` |

Базовые команды для повторения среза:

```powershell
py -3 -m pytest -q
py -3 -m tfl eval
```

## Граница версии 1.0

Библиотека оракулов, предметные рецепты, автономная граница проектов ЛР,
пакетная переносимость и harness сквозной оценки работоспособны. Это завершённая
база помощника, а не обещание разрешить любой неразрешимый класс задач.

Полный baseline LLM-цикла, Claude smoke после снятия внешнего auth-блокера и
часть предметных исследований остаются полезными проверками, но не блокируют
обычное использование 1.0. Проверить неизвестные сейчас условия ЛР и слепые
задачи 2026 невозможно до их публикации. Эти пункты сохранены в
`docs/OPEN-GAPS.md` как наблюдаемая граница и очередь сопровождения, а не как
план непрерывной разработки.

## Источники и память

- Официальные материалы: `references/teacher/FormalLanguageTheory/`.
- Учебные материалы и сканы: `references/course/TFL-IU9-claude/`.
- Безопасное текстовое зеркало: `corpus/txt/`.
- Обезличенные уточнения и приёмы: `corpus/knowledge/`.

Если локальные референсы отсутствуют в свежем клоне, агент продолжает работу
по версионируемому `corpus/`, явно отмечая невозможность сверить оригинал.

Первый сквозной smoke-run сохранён в
`reports/agent-holdout/codex-smoke-2026-09-12.md`: 15 наблюдаемых команд,
197,57 с, 10/10. Второй (`exam-2024-b15-q1`) после введения бюджета использовал
9 из 10 команд, 227,53 с и 294894 входных токена, также 10/10. Полный baseline
из семи случаев ещё не снят.

Claude adapter проверен вплоть до реального запуска CLI 2.1.205, разбора
event-stream и API-запроса. Организация отклоняет модель до первого tool call
с 403 `oauth_org_not_allowed`; это внешний auth-блокер, а не пропущенный PASS.
