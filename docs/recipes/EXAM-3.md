# EXAM-3 — третий вопрос билета: ротирующийся слот

Третий вопрос обычно называют «теоретическим», и это неточно. Замер
по 255 вопросам (`py -3 tools/exam_survey.py`):

| тема | сколько из 85 | механизировано |
|---|---|---|
| замкнутость классов относительно операций | 47 | проверка контрпримера |
| атрибутная грамматика для языка | 11 | да, `tfl/attr.py` |
| завершимость SRS | 7 | да, **6 из 7 закрыто** |
| НКА для пересечения + минимальность | 5 | да |
| синтаксический моноид | 4 | да, `tfl/monoid.py` |
| LL/LR-свойство | 3 | частично |
| регулярка для дополнения | 3 | да |
| конъюнктивная грамматика | 3 | да, `tfl/conj.py` |
| таблица классов, проблема Поста | по 1 | да, `tfl/myhill.py`, `tfl/pcp.py` |

Больше трети третьих вопросов — обычные построения, а не рассуждения
о классах. Открывайте билет и читайте, а не готовьтесь к «теории».

---

## A. Завершимость SRS (7 вопросов)

Условие даётся со степенями: `ab3` означает `abbb`. Раскрываете
и отдаёте оракулу:

```python
from tfl.srs import parse_srs
parse_srs("abb -> abbb\nbbba -> aaab\naabb -> bbaa").terminates()
```

Все семь систем из билетов и что вышло:

| билет | система | вердикт | свидетель |
|---|---|---|---|
| 3 | `abb→ab³`, `b³a→a³b`, `a²b²→b²a²` | **не завершается** | петля `abb → abbb` |
| 7 | `bab→aba`, `a³→b³`, `a²b²→b²a²` | **не завершается** | цикл из 8 слов |
| 8 | `b²a²→ba³`, `a²→ba` | **не завершается** | цикл `baaa → bbaa → baaa` |
| 11 | `abc→b²a`, `ac→ab`, `a²b²c²→c²b²a²c²` | **завершается** | `[a]=2x`, `[b]=x+1`, `[c]=x+2` |
| 17 | `abc→b²a²`, `ba→ab`, `a²b²c²→cb²a²c` | **завершается** | `[a]=x+1`, `[b]=2x`, `[c]=2x+5` |
| 22 | `abc²→b²a²`, `b²→a²`, `a²b²c²→c²b²a²` | **завершается** | `[a]=x`, `[b]=2x+1`, `[c]=2x+2` |
| 70к | `fgh→h³g³f³`, `h²→fg²hg²f` | не выяснено | — |

### Петля, а не цикл

Билет 3 показывает, почему поиска цикла мало. Правило `abb → abbb`
не возвращает ни одно слово к самому себе: слово просто растёт.
Оракул ищет **петлю** `w →⁺ u·w·v`, а цикл — её частный случай при
`u = v = ε`. Вывод правомерен: тем же выводом внутри вложенного `w`
получается `u²·w·v²`, и так без конца.

Самая дешёвая проверка на глаз: **не вложена ли левая часть правила
в правую**. Если да — система не завершается, и это одна строка ответа.

### Интерпретации против удлиняющих правил

Три системы из семи завершимы, и ни у одной нет убывающего армейского
порядка: у всех есть удлиняющее правило. Работает линейная интерпретация
`x ↦ m·x + c` с `m ≥ 1`, где слово — **композиция** функций своих букв.
Правило убывает, если `M_l ≥ M_r` и `C_l > C_r`.

Записывать в ответ надо саму интерпретацию и проверку на каждом правиле —
это и есть доказательство.

### Теоретический вариант того же вопроса

> **Билет 1.** Если `τ₁` и `τ₂` — завершимые TRS, будет ли завершимой TRS,
> включающая все правила из обеих?

Ответ «нет», контрпример из двух однобуквенных систем: `a → b` и `b → a`.
Каждая завершима (одно правило, слово уменьшается по порядку `a > b`),
объединение даёт цикл `a → b → a`, что оракул и подтверждает.

### Приёмы, которые остаются, когда не хватило интерпретации

Разложены в `docs/recipes/LAB-1.md`, §1a–§1e; здесь порядок вызова:

```python
system.prove_by_dependency_pairs()   # пары зависимостей
system.find_matrix_interpretation()  # матрица над ℕ
system.prove_by_removal()            # удаление правил, включает арктику
system.prove_by_match_bound(3)       # ограничение совпадениями
system.terminates_by_exhaustion(12)  # не удлиняющая система: точно на коротких словах
```

Что стоит сказать в ответе про **границу**: арктическая интерпретация
и ограничение совпадениями дают **линейную** оценку длины вывода,
поэтому системе с квадратичным выводом (`ab → ba` на `aⁿbⁿ`) они
недоступны в принципе, а обычная матрица размерности 2 её берёт.

## A′. Копредставления полугрупп и групп

Лекция 1 курса 2026, слайды 4–6. Соотношение это равенство, у него нет
направления; система переписывания направление имеет. Отсюда весь приём:
**ориентировать** соотношения армейским порядком, пополнить
по Кнуту–Бендиксу и получить полную систему.

```python
from tfl.presentation import parse_presentation
p = parse_presentation("группа: a, b
ab = ba")
p.equal("abab", "aabb")   # проблема равенства, три исхода
p.is_commutative()        # достаточно проверить образующие
p.is_trivial()            # все ли образующие равны единице
```

Обратный элемент пишется заглавной буквой (`A` это `a⁻¹`), ввод понимает
и `a^-1`, и `a'`; у группы автоматически добавляются правила свободного
сокращения `aA → ε`, `Aa → ε`.

Три вещи, которые стоит написать в ответе явно.

1. **Почему после пополнения ответ точный.** Каждое правило строго
   убывает в армейском порядке, порядок фундирован — значит система
   завершима; пополнение сошлось — значит все критические пары
   соединяются, то есть система локально конфлюэнтна; по **лемме
   Ньюмана** отсюда конфлюэнтность, а у завершимой конфлюэнтной системы
   нормальная форма единственна.
2. **Равенство доказывается и без конфлюэнтности.** Совпадение
   нормальных форм — уже готовая цепочка соотношений, применённых
   в обе стороны. Пример: у `⟨a, b | aba⁻¹ = b², bab⁻¹ = a²⟩` пополнение
   расходится, а тривиальность группы доказывается — обе образующие
   приводятся к пустому слову.
3. **«Не равны» без полной системы сказать нельзя.** Слова могли бы
   сойтись дальше по обходу. Вердикт это различает.

Ориентация выбирается, а не читается из записи. Полугруппа Цейтина
(слайд 5) слева направо зацикливается (`cca → ccae`), а по армейскому
порядку завершима; пополнение на ней всё равно расходится — как
и обязано, проблема равенства там неразрешима.

### Граф Кэли: тот же вопрос, но про язык

Если пополнение сошлось, у каждого элемента одна нормальная форма,
и граф Кэли строится обходом: вершины — нормальные формы, рёбра —
умножение справа. Это **детерминированный автомат**, и с финальной
единицей он распознаёт язык проблемы равенства.

```python
p.is_finite()               # разрешимо после пополнения
p.cayley().witness.dfa()    # автомат проблемы равенства
```

Два факта, которые в ответе стоят дороже самой конструкции.

* **Классов Майхилла–Нероуда ровно столько, сколько элементов.** Вычет
  по слову `u` — это `{v : uv = 1}`, то есть слова, равные `u⁻¹`; значит
  вычеты и элементы группы это одно и то же. Отсюда лёгкая половина
  **теоремы Анисимова**: язык проблемы равенства регулярен тогда
  и только тогда, когда группа конечна.
* **Конечность разрешима.** Элементы — это неприводимые слова, они
  образуют регулярный язык (в нём нет подслов-левых частей), а конечность
  регулярного языка проверяется отсутствием цикла. Поэтому «бесконечна»
  предъявляется циклом, а не отговоркой «обход не закрылся».

Действие группы (`tfl.cayley.Action`) стоит рядом и делает то, чего
переписывание не умеет: **доказывает неравенство** без полной системы.
Образующие идут в перестановки точек, соотношения обязаны действовать
тождественно (проверка полная), и если образ различает два слова —
они не равны.

## B. Замкнутость классов (47 вопросов)

Формулировки: «Верно ли, что…», «Может ли…», «Всегда ли…», «Если `L` —
LL(1)-язык, будет ли `Lᴿ` детерминированным?».

**Ответ почти всегда «нет», и весь вопрос в контрпримере.** Механизировать
поиск контрпримера нельзя, а вот **проверить** найденный — можно и нужно:
собрать оба языка предикатами, построить результат операции и прогнать
через тот же набор оракулов, что и в EXAM-1/EXAM-2.

Рабочий запас контрпримеров, все проверяемые:

| нужен | берут |
|---|---|
| DCFL, чей реверс не DCFL | `{aⁿbⁿcᵐ} ∪ {aⁿbᵐcᵐ}` и его реверс |
| два DCFL, чьё объединение не DCFL | `{aⁿbⁿcᵐ}` и `{aⁿbᵐcᵐ}` |
| не-КС, чья конкатенация регулярна | `L` и его дополнение в подходящем виде |
| завершимые SRS, чьё объединение зацикливается | `a → b` и `b → a` |

Про «в непересекающихся алфавитах» стоит помнить отдельно: там многие
операции ведут себя лучше, и ответ бывает «да». Проверяйте, не переносите
привычку.

## C. Синтаксический моноид (4 вопроса)

```python
from tfl.automata import dfa_of
from tfl.monoid import syntactic_monoid

syn = syntactic_monoid(dfa_of("(a|b)*a(a|b)", alphabet="ab").minimize())
len(syn)              # 7
syn.is_aperiodic()    # True
print(syn.markdown()) # таблица элементов
print(syn.srs())      # представление системой переписывания
```

Проверенные билеты:

| билет | язык | состояний ДКА | элементов моноида | апериодичен |
|---|---|---|---|---|
| 2 | `(b(a|b)*a(a|b))*b` | 7 | 17 | да |
| 19 | `(a|b)*a(a|b)` | 4 | 7 | да |
| 23 | `(aa|bb)*ab` | 4 | 21 | **нет** |

Апериодичность по Шютценберже равносильна беззвёздности языка,
и в билете 23 её нет: язык требует чётности блоков, а чётность
беззвёздным выражением не описывается.

**Осторожно со счётом состояний.** Синтаксический моноид — это моноид
трансформаций **минимального полного** ДКА, вместе с ловушкой. У билета 23
`len(dfa)` даёт 4, а элементы моноида отображают пять состояний `0…4`:
пятое — ловушка. Выбросите её — получите другой моноид.

Отличие моноида трансформаций от синтаксического — вопрос из банка
«Фармы» (`corpus/issues/pharma-issues9-27-34.md`), и он же +1 балл
на второй задаче РК1.

## D. НКА для пересечения с обоснованием минимальности (5 вопросов)

> **Билет 13.** Построить НКА для пересечения языка `(aa|bb)*(ba|ab)`
> и языка грамматики `S → ε | aA; A → b | aB; B → aA`.
> Частично обосновать минимальность.

Порядок: перевести оба языка в автоматы, взять произведение, редуцировать,
затем **обосновать** нижнюю оценку. Обоснование делает
`tfl.myhill.extended_fooling_set` — **треугольная** версия критерия
Глайстера–Шаллита. Симметричная версия систематически занижает оценку,
и брать надо именно расширенную.

## E. Атрибутная грамматика для языка (11 вопросов)

> Привести атрибутную грамматику для `{a^(2ⁿ) | n > 0}`, не используя
> умножение и возведение в степень при обработке атрибутов.

Инструмент — `tfl/attr.py` (нотация курса `S0`/`S1`, `:=` и условия,
синтезируемые и наследуемые атрибуты, перебор деревьев вывода,
`check_attribute`). Подробный разбор — `docs/recipes/RK2-C.md`,
там же счёт по неподвижной точке для грамматик с ε-циклами.

Ограничение «без умножения» — не украшение: оно заставляет считать
удвоение сложением, то есть переносить значение по дереву, а не вычислять
его в узле.

## F. Конъюнктивные грамматики (3 вопроса)

> **Билет 23 (2024).** Построить конъюнктивную грамматику для языка
> `{a^i₁ b a^i₂ b … b a^iⁿ | k > 0 ⇒ i_j < i_{j+k}}`.

Условие читается как «длины блоков строго возрастают». Грамматика,
сверенная с предикатом на всех словах до длины 9:

```
S → D & T | A        D → P a M        P → a P a | b
M → a M | G          G → ε | b Z      Z → a Z | b Z | ε
T → a T | b S        A → a A | ε
```

Приём: один конъюнкт сравнивает **первую пару** блоков, второй требует,
чтобы **хвост после первого `b` сам лежал в языке**, а конъюнкция сшивает
их на одном и том же слове. Подробный разбор, включая автоматы Треллиса
и линейные конъюнктивные грамматики, — `docs/recipes/RK2-B.md`, шаг 6.

Инструмент — `tfl/conj.py`: обобщённый CYK по неподвижной точке, проверка
линейности, автомат Треллиса и перевод его в грамматику по лекции.
Оба примера лекции (`{(aⁿb)ᵏ}` и «язык равенства» `{wcw}`) воспроизводятся.

## G. Проблема соответствия Поста

Третьим вопросом билета 2024 стоит «Решить проблему соответствия Поста
⟨ab, bba⟩, ⟨aba, a⟩, ⟨b, ba⟩». Разбор — `tfl/pcp.py`, подробно
в `docs/recipes/PHARMA.md`, раздел «Проблема соответствия Поста».

Порядок действий: сперва счётные уравнения на числа домино (дёшево
и часто закрывает вопрос сразу), затем усиление уравнениями на пары
букв, и только потом поиск.

На самом билете 2024 счётные уравнения не срабатывают, а усиленная
система решение **запрещает**: последним может быть только домино 2,
из уравнений на пары следует `N₂₃ = 0` и первое ≠ 3, значит первое —
домино 2, но единственное возможное продолжение после него это домино 3.
Противоречие. В отчёт идёт эта цепочка, а не «перебором не нашлось».

## H. Что остаётся ручным

* **µ-выражения — 1 вопрос** (в первом вопросе билета 2022).

Для PCP общий алгоритм невозможен, но конкретный билет разбирается
`tfl/pcp.py`: инварианты могут доказать отсутствие решения, а найденная
последовательность домино проверяется независимо. Исчерпание бюджета поиска
само по себе отсутствия решения не доказывает.

---

## Границы выводов

| Что получено | Что доказано |
|---|---|
| найдена петля или цикл | **система не завершается** |
| найдена убывающая интерпретация | **система завершается** |
| интерпретации нет в диапазоне | ничего: диапазон узкий |
| контрпример к замкнутости проверен оракулом | **класс не замкнут** |
| контрпример не нашёлся | ничего: искать контрпример механически мы не умеем |
| моноид посчитан | это **доказательство**, конструкция конечная и точная |

## Чего не делать

* Не готовиться к третьему вопросу как к «теории»: больше трети — построения.
* Не искать цикл там, где нужна петля: правило с вложенной левой частью
  циклов не даёт вовсе.
* Не выбрасывать ловушку при подсчёте синтаксического моноида.
* Не брать симметричную версию критерия Глайстера–Шаллита.
* Не отвечать «да» на вопрос о замкнутости без доказательства: «нет»
  подтверждается контрпримером, а «да» требует построения в общем виде.

## Чек-лист

- [ ] Прочитано, что именно спрашивают — тема за номером не закреплена.
- [ ] Для SRS: степени раскрыты, проверена вложенность левой части в правую.
- [ ] Для SRS: вердикт подкреплён петлёй или интерпретацией, а не словами.
- [ ] Для замкнутости: контрпример предъявлен и проверен оракулом.
- [ ] Для моноида: сказано, минимальный ли полный ДКА взят за основу.
- [ ] Для НКА: минимальность обоснована треугольным критерием.
