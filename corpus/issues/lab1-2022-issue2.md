# Issue #2 — ЛР1, 2022 (Кнут–Бендикс, Мартелли–Монтанари, классы грамматик)

Источник: `github.com/TonitaN/FormalLanguageTheory/issues/2`, сентябрь 2022.
Слой **A**.

В 2022 году ЛР1 была другой, чем в 2025: три варианта по модулю 3 —
унификация (0), классы эквивалентности нетерминалов (1), Кнут–Бендикс (2).
К нынешней ЛР1 (SRS над трансформационным моноидом) тред прямого отношения
не имеет, но два разбора здесь общего значения: порядок Кнута–Бендикса
на переменных и алгоритм 3 Мартелли–Монтанари с полным примером.

Персональные данные (списки бонусов по фамилиям, передача лаб на доработку,
ники) не переносятся.

## Кнут–Бендикс: переменные

> Для любой `f` из множества конструкторов и любого `x` из множества
> переменных ⇒ `f(x) > x`.
>
> Можно и более сильное высказывание сделать: любое выражение, содержащее
> переменную, больше, чем эта переменная. Но, например, `f(x) > y` уже
> не верное утверждение. Две переменные между собой также не сравнимы.

Про правила вида `y ::= f(x)`, где справа переменная, которой нет слева:

> Переменная — не конструктор, потому что на них порядок не выстраивается
> вообще. Поэтому такие правила, в которых справа есть переменная, которой
> нет слева, точно не завершаемые по Кнуту–Бендиксу. Впрочем, они
> не завершаемые в принципе (чтобы убедиться, достаточно подставить вместо
> этой переменной там опять левую часть правила, и продолжать этот процесс
> сколько угодно раз).

## Классы эквивалентности нетерминалов: инкрементальность

Ответ на «можно ли удалить все правила с непорождающими нетерминалами»:

> В рамках лабы нельзя. Потому что алгоритм по смыслу должен подходить
> для инкрементальной обработки грамматик. Если вы удалите правила
> с некоторым нетерминалом слишком рано, а потом добавится правило
> на этот нетерминал и он станет порождающим, классы эквивалентности
> уже станут не адекватны.
>
> Приравнивать все **синтаксически** непорождающие нетерминалы (т.е. такие,
> для которых нет правил, где они стоят в левой части) можно и нужно
> (т.к. они соответствуют одному классу эквивалентности — с пустым
> множеством терминальных форм). Но приравнивать все нетерминалы,
> ссылающиеся на непорождающие нетерминалы, уже нет.

Формат ответа: классы эквивалентности + новая грамматика с представителями.

> Не забываем, что сравниваются **множества**, а не упорядоченные списки
> терминальных форм.

## Мартелли–Монтанари: именно алгоритм 3 (19.09)

> Нужен именно настоящий Мартелли–Монтанари, а не из википедии!
> Для сомневающихся: см. статья указанных авторов. Нам нужен **алгоритм 3**,
> а не алгоритм 1 (который приведён там лишь для примера и чтобы показать,
> где этот стандартный алгоритм не эффективен).

Три правила, сформулированные в ответе:

1. **Старт.** Рассматривается система `{xi} = {}`, `{X} = {t1, t2}`,
   где `X` — свежая переменная, `t1`, `t2` — входные термы,
   `xi` — переменные из `t1` или `t2`.
2. **Выбор уравнения.** Нужно выбрать такое, в котором в правой части есть
   хотя бы два терма, и притом ни одна переменная левой части которого
   не встречается в правой части никакого уравнения вообще. Мультиуравнение
   имеет вид `S = M`, где `S` — множество переменных, `M` — множество термов,
   **не являющихся переменными** (это необходимо проверять).
   Если остались только уравнения с одним термом справа, можно выбрать такое
   без зависимостей в правых частях и сразу вынести его в итоговую подстановку.
3. **Компактификация.** Ищем все мультиуравнения вида `S1 ∪ {xi} = M1`,
   `S2 ∪ {xi} = M2` (один и тот же `xi`) и объединяем в
   `S1 ∪ {xi} ∪ S2 = M1 ∪ M2`. Желательно так же и с правыми частями:
   найти все пары `S1 = M1 ∪ {ti}`, `S2 = M2 ∪ {ti}` и объединить.

### Полный пример из ответа

Пара `F(q,q,q)` и `F(F(x,y,R), F(a,w,a), F(w,x,y))`; строчные — переменные,
заглавные — конструкторы.

Первый этап — система:

```
{x0} = {F(q,q,q), F(F(x,y,R),F(a,w,a),F(w,x,y))}
{q} = {}   {x} = {}   {y} = {}   {a} = {}   {w} = {}
```

Общая часть `F(q,q,q)`, граница даёт `{q} = {F(x,y,R)}`, `{q} = {F(a,w,a)}`,
`{q} = {F(w,x,y)}`. После компактификации по `q`:

```
{q} = {F(x,y,R), F(a,w,a), F(w,x,y)}
{x} = {}   {y} = {}   {a} = {}   {w} = {}
```

Общая часть `F(x,y,a)` (подстановка `{q} := F(x,y,a)`), граница:
`{x,a,w} = {}`, `{y,w,x} = {}`, `{a,y} = {R}`. Компактификация даёт
`{x,a,w,y} = {R}` — итоговая подстановка.

Результат: терм `F(q,q,q)` с подстановками `{q} := F(x,y,a)`,
`{x,a,w,y} = R`.

> Ещё стоит обратить внимание на уравнения вида `{xi, xj} = {}`. Они не задают
> никакой общей части, поэтому шаг унификации к ним не применяется,
> но в итоговую подстановку эти уравнения нужно будет перенести после
> завершения всех шагов (т.к. они определяют подстановку `xi := xj`).
