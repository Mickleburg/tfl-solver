
----- [page 1] -----
Дедлайнерская Лаб. работа №1
Базовая стоимость этой лабораторной работы — 6
баллов.
1 (ваш номер — 0 в поле Z3) Автоматическое
построение таблицы классов эквивалентности по
Майхиллу–Нероуду по ДКА.
2 (ваш номер — 1 в поле Z3) Построение пересечения
регулярной и контекстно-свободной грамматики.
3 (ваш номер — 2 в поле Z3) LL-корректное
устранение ε-правил в грамматике.
1 / 6
▲

----- [page 2] -----
Синтаксис входных данных
1 (0 в поле Z3) Синтаксис ДКА такой же, как в
нечётном варианте 2 лабораторной работы, но
состояния-ловушки могут не присутствовать в
автомате явно. Гарантируется, что по каждой букве
из каждого состояния не больше одного перехода.
2 (1 в поле Z3) Синтаксис КС-грамматики такой же,
как в чётном варианте 3 лабораторной работы (и так
же в псевдо-GNF и без ε-правил). Синтаксис
регулярной грамматики такой же, как в чётном
варианте 2 лабораторной работы (строго
праволинейный, без ε-правил). Читаются из двух
отдельных файлов, CFG_x.txt и RG_x.txt.
3 (2 в поле Z3) Синтаксис КС-грамматики такой же,
как в чётной 4 лабораторной работе.
2 / 6
▲

----- [page 3] -----
Извлечение классов эквивалентности
1
В ДКА находятся все пути из начального состояния в другие
состояния, которые не проходят дважды ни через одну вершину.
Слова, распознаваемые вдоль таких путей, считаются потенциальными
классами эквивалентности. Если на этом этапе вдоль разных путей
получились одинаковые слова, необходимо отбросить дубликаты.
2
Для каждого построенного слова β подбираются два суффикса γ1 и
γ2: так что слово βγ1 распознаётся автоматом, а слово βγ2 не
распознаётся. Иногда такие γ1 или γ2 не удастся построить. Поэтому
необходимо определить множество состояний-ловушек и множество
«совершенно конечных» состояний (тех, переходы из которых
делаются только в конечные состояния). Если слово β распознаётся в
ловушке, γ1 не существует; если в совершенно конечном состоянии –
не существует γ2. Если хотя бы одно из них построить не удалось,
тогда не добавляем в таблицу ничего.
3
Добавить в список суффиксов ε. Для каждого построенного префикса
βi и суффикса γj проверить, распознаётся ли слово βiγj ДКА.
Представить таблицу-результат в формате markdown.
3 / 6
▲

----- [page 4] -----
Построение пересечения грамматик
1 Базовый алгоритм можно взять из лекций (лекция 7).
2 По исчерпанию удалить все недостижимые и
непорождающие нетерминалы и содержащие их правила.
3 До неподвижной точки применить алгоритм
альфа-преобразования: если для всех правил,
содержащих нетерминал A, можно заменить A на B, и
при этом получатся уже существующие правила
грамматики, тогда заменить A на B во всех правилах и
удалить все дубликаты.
4 / 6
▲

----- [page 5] -----
LL-корректное удаление ε-правил
1
Базовый алгоритм можно взять из лекций (лекция 9). До его
применения удалить все недостижимые и непорождающие
нетерминалы и содержащие их правила. Учесть, что исходная
грамматика допускает нетерминалы вида [N1N2], поэтому при
присоединении контекста использовать разделитель (например,
+, или по Дьячеку, ->).
2
Если грамматика не LL(k), тогда алгоритм может зациклиться.
Произойти это может, по лемме Розенкранца и Стирнса, только
если в результате присоединения обнуляемого контекста
возникнет нетерминал вида
[(Φ1−>)?A(−> Φ2)?−> A(−> Φ3)?] (т.е. такой, в котором
дважды присоединяется один и тот же обнуляемый
нетерминал). В таких случаях прерывать исполнение, печатать
промежуточную грамматику, в которой появился проблемный
нетерминал, и сообщать, что исходная грамматика — не LL(k).
5 / 6
▲

----- [page 6] -----
Дополнительные баллы
1 (0 в поле Z3) (+2) Отрисовывать ДКА в виде
dot-графа, а таблицу — в LATEX-формате, после чего
собирать их в единый LATEX-файл c двумя рисунками
(окружение figure): автомат и таблица его классов.
2 (1 в поле Z3) (+2) На вход алгоритма может
приниматься как регулярная грамматика, так и
конечный автомат, описанный в синтаксисе 2
лабораторной работы. В этом случае входной файл
будет DFA_x.txt.
3 (2 в поле Z3) (+2) Проверить исходную и
результирующую грамматику на LL(1)-свойства,
стандартным алгоритмом построения LL-таблицы.
6 / 6
▲
